/**
 * components/dashboard/StatusBadge.tsx
 *
 * Thin re-export. The implementation lives in lib/video-status.ts so it can
 * be shared between server components and client components without duplication.
 */

export { StatusBadge } from '@/lib/video-status'
